# WSL2 Local Infra

This directory contains all local setup/infra customizations for the Windows + WSL2 workflow.

## Commands

Run all local infra commands with:

```bash
make -f .wsl2/Makefile help
```

## Compare Against Upstream Main

From repo root:

```bash
make -f .wsl2/Makefile compare-main
```
