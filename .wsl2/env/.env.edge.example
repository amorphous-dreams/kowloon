# Shared edge proxy settings for local QA routing.
# Production runs externally and should maintain its own edge config.
# Copy to wsl2/env/.env.edge and adjust for your host.

EDGE_NETWORK=kowloon-edge

# Local operator convenience. Set false if this endpoint is exposed publicly.
TRAEFIK_API_INSECURE=true

# Required for Let's Encrypt certificate issuance.
ACME_EMAIL=admin@example.com

# Router label resolver name used by the QA compose file.
TRAEFIK_CERT_RESOLVER=le
